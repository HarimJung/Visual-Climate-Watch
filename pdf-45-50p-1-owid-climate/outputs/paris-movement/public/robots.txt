# Every record here is meant to be cited, so every record is meant to be found.
# The per-country pages carry their own canonical and a description built from
# the record's own figures, so what a result quotes is the claim the page makes.
#
# There is no sitemap: /countries server-renders all 218 records as links, so
# the collection is reachable by crawling rather than by being told.
#
# Every record is also available as JSON at /api/v1/country-dial?country=<ISO3>.
User-agent: *
Allow: /
