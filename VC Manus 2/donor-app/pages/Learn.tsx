import { ArrowRight, BookOpen, CheckCircle2, Clock3, Download, GraduationCap, LockKeyhole } from "lucide-react";
import { Link } from "wouter";
import { trpc } from "@/lib/trpc";
import EditorialShell from "@/components/EditorialShell";

export default function Learn() { const { data } = trpc.climate.learning.useQuery(); return <EditorialShell eyebrow="08 · LEARN / BUILD CLIMATE DATA FLUENCY"><div className="page-intro"><div><div className="section-kicker">LEARN WITH EVIDENCE</div><h1>Build climate<br /><em>data fluency.</em></h1><p>Short, practical paths built around the same source, comparability, and quality rules used across Visual Climate.</p></div><div className="learning-badge"><GraduationCap size={20} /><strong>Three paths</strong><small>from foundations to finance</small></div></div><section className="learning-grid">{data?.paths.map((path, index) => <article className={`learning-card tone-${index}`} key={path.id}><div className="learning-card-top"><div className="learning-icon"><BookOpen size={18} /></div><span>{path.level}</span></div><h2>{path.title}</h2><div className="learning-meta"><Clock3 size={13} /> {path.duration} · {path.steps.length} steps</div><div className="learning-steps">{path.steps.map((step, stepIndex) => <div key={step}><span>0{stepIndex + 1}</span>{step}</div>)}</div><button className="button-primary">Start path <ArrowRight size={14} /></button><button className="button-quiet"><Download size={14} /> Download offline</button></article>)}</section><section className="learning-method editorial-panel"><LockKeyhole size={18} /><div><strong>Offline and on purpose.</strong><p>Learning content can be cached locally. Live data, connector checks, and AI analysis require an internet connection. Sign in to sync progress.</p><Link href="/account" className="text-link">Open account workspace <ArrowRight size={13} /></Link></div></section><div className="methodology-note"><CheckCircle2 size={16} /><div><strong>PSEA method.</strong> Prepare · Study · Explain · Apply. Every path ends by returning to a real Visual Climate workflow.</div></div></EditorialShell>; }
